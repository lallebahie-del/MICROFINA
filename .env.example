## MICROFINA local environment example
## Copy to ".env" (do not commit .env) then run:
##   docker compose up -d
##
## SQL Server SA password (must be complex)
MSSQL_SA_PASSWORD=Password123!

## Optional: override backend DB connection when running locally
# MICROFINA_DB_URL=jdbc:sqlserver://localhost:1434;databaseName=microfina;encrypt=true;trustServerCertificate=true
# MICROFINA_DB_USERNAME=sa
# MICROFINA_DB_PASSWORD=Password123!

