<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
    xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.27.xsd">

    <!--
        P3-001 – Model_Compte_Produit_Credit
        Accounting model template for a credit product; drives the chart-of-accounts
        entries generated when a credit operation is posted.
        No external FK dependencies.
        DDL source of truth: Phase 3 spec – Section 1 (produitcredit accounting model).
    -->
    <changeSet id="P3-001-create-Model_Compte_Produit_Credit" author="microfina">

        <createTable tableName="Model_Compte_Produit_Credit">

            <!-- PK -->
            <column name="ID_MODEL_PRODUIT_CREDIT" type="BIGINT" autoIncrement="true">
                <constraints primaryKey="true"
                             primaryKeyName="PK_Model_Compte_Produit_Credit"
                             nullable="false"/>
            </column>

            <!-- Label -->
            <column name="LIBELLEMODELPRODUITCREDIT" type="NVARCHAR(255)">
                <constraints nullable="true"/>
            </column>

            <!-- 1 = model is backed by an earmarked resource -->
            <column name="ressource_affecte" type="INT">
                <constraints nullable="true"/>
            </column>

            <!-- 1 = Islamic finance model (Mourabaha / Moucharaka / Ijara) -->
            <column name="model_islamic" type="INT">
                <constraints nullable="true"/>
            </column>

            <!-- Optimistic lock -->
            <column name="VERSION" type="INT" defaultValueNumeric="0">
                <constraints nullable="false"/>
            </column>

        </createTable>

        <rollback>
            <dropTable tableName="Model_Compte_Produit_Credit"/>
        </rollback>

    </changeSet>

</databaseChangeLog>
